<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- Layout and common templates -->
        <template id="assets_frontend_luonto" inherit_id="website.assets_frontend" name="Shop Extended">
            <xpath expr="link[last()]" position="after">
                <link rel="stylesheet" type="text/css" href="/luonto_product/static/src/css/luonto_style.css"/>
            </xpath>
            <xpath expr="script[last()]" position="after">
                <script type="text/javascript" src="/luonto_product/static/src/js/shop_attribute.js"/>
            </xpath>
        </template>

        <template id="assets_backend_luonto" inherit_id="sale.assets_backend_inherit_sale" name="Configurator Extended">
            <xpath expr="link[last()]" position="after">
                <link rel="stylesheet" type="text/css" href="/luonto_product/static/src/css/luonto_style.css"/>
            </xpath>
            <xpath expr="script[last()]" position="after">
                <script type="text/javascript" src="/luonto_product/static/src/js/product_configurator.js"/>
                <script type="text/javascript" src="/luonto_product/static/src/js/product_configurator_controller.js"/>
            </xpath>
        </template>

        <template id="variants_inherit_luonto" inherit_id="sale.variants" name="Variants Extended">
            <xpath expr="//ul" position="replace">
                <t t-set="no_buy_attrs" t-value="product.get_attr_no_buy()"/>
                <t t-set="flat_ex_attrs" t-value="product.get_flat_exclusions()"/>
                <ul t-attf-class="list-unstyled js_add_cart_variants #{ul_class}"
                    t-att-data-attribute_exclusions="json.dumps(attribute_exclusions)"
                    t-att-data-no_buy="json.dumps(no_buy_attrs)"
                    t-att-data-flat_ex="json.dumps(flat_ex_attrs)">
                    <t t-foreach="product._get_valid_product_template_attribute_lines()" t-as="variant_id">
                        <!-- Attributes selection is hidden if there is only one value available and it's not a custom value -->
                        <li t-att-data-attribute_id="variant_id.attribute_id.id"
                            t-att-data-attribute_name="variant_id.attribute_id.name"
                            t-attf-class="variant_attribute #{'d-none' if len(variant_id.product_template_value_ids) == 1 and not variant_id.product_template_value_ids[0].is_custom else ''}">

                            <strong t-field="variant_id.attribute_id.name" t-attf-class="attribute_name #{'first_select' if variant_id_first else ''}"/>
                            <t t-if="variant_id_first">
                                <a role="button" id="reset_prod" class="btn btn-primary btn-lg reset_button" href="#">Reset</a>
                            </t>

                            <t t-if="variant_id.attribute_id.type == 'select'">
                                <select
                                        t-att-data-attribute_id="variant_id.attribute_id.id"
                                        t-attf-class="form-control js_variant_change #{variant_id.attribute_id.create_variant}"
                                        t-att-name="'attribute-%s-%s' % (product.id, variant_id.attribute_id.id)">
                                    <t t-foreach="variant_id.product_template_value_ids.filtered(lambda p: p.product_attribute_value_id.is_not_buy)" t-as="please_sel">
                                        <option t-att-value="please_sel.id"
                                                t-att-data-value_id="please_sel.product_attribute_value_id.id"
                                                t-att-data-value_name="please_sel.name"
                                                t-att-data-attribute_name="please_sel.attribute_id.name"
                                                t-att-data-is_custom="please_sel.is_custom"
                                                t-att-data-is_init="True"
                                                t-att-selected="first_possible_combination[variant_id_index] == please_sel">
                                            <span t-field="please_sel.name"/>
                                        </option>
                                    </t>
                                    <t t-foreach="variant_id.product_template_value_ids.filtered(lambda p: not p.product_attribute_value_id.is_not_buy).sorted(lambda v: v.name)" t-as="value_id">
                                        <option t-att-value="value_id.id"
                                                t-att-data-value_id="value_id.product_attribute_value_id.id"
                                                t-att-data-value_name="value_id.name"
                                                t-att-data-attribute_name="value_id.attribute_id.name"
                                                t-att-data-is_custom="value_id.is_custom"
                                                t-att-selected="first_possible_combination[variant_id_index] == value_id">
                                            <span t-field="value_id.name"/>
                                        </option>
                                    </t>
                                </select>
                            </t>

                            <t t-if="variant_id.attribute_id.type == 'radio'">
                                <ul t-att-data-attribute_id="variant_id.attribute_id.id" class="list-unstyled">
                                    <t t-foreach="variant_id.product_template_value_ids" t-as="value_id">
                                        <li class="form-group js_attribute_value" style="margin: 0;">
                                            <label class="col-form-label">
                                                <div>
                                                    <input type="radio"
                                                           t-attf-class="js_variant_change radio_input #{variant_id.attribute_id.create_variant}"
                                                           t-att-checked="first_possible_combination[variant_id_index] == value_id"
                                                           t-att-name="'attribute-%s-%s' % (product.id, variant_id.attribute_id.id)"
                                                           t-att-value="value_id.id"
                                                           t-att-data-value_id="value_id.product_attribute_value_id.id"
                                                           t-att-data-value_name="value_id.name"
                                                           t-att-data-attribute_name="value_id.attribute_id.name"
                                                           t-att-data-is_custom="value_id.is_custom"/>
                                                    <div class="radio_input_value">
                                                        <span t-field="value_id.name"/>
                                                    </div>
                                                </div>
                                            </label>
                                        </li>
                                    </t>
                                </ul>
                            </t>

                            <t t-if="variant_id.attribute_id.type == 'color'">
                                <ul t-att-data-attribute_id="variant_id.attribute_id.id" class="list-inline">
                                    <li t-foreach="variant_id.product_template_value_ids" t-as="value_id"
                                        class="list-inline-item">
                                        <label t-attf-style="background-color:#{value_id.html_color or value_id.name if not value_id.is_custom else ''}"
                                               t-attf-class="css_attribute_color #{'active' if first_possible_combination[variant_id_index] == value_id else ''} #{'custom_value' if value_id.is_custom else ''}">
                                            <input type="radio"
                                                   t-attf-class="js_variant_change  #{variant_id.attribute_id.create_variant}"
                                                   t-att-checked="first_possible_combination[variant_id_index] == value_id"
                                                   t-att-name="'attribute-%s-%s' % (product.id, variant_id.attribute_id.id)"
                                                   t-att-value="value_id.id"
                                                   t-att-title="value_id.name"
                                                   t-att-data-value_id="value_id.product_attribute_value_id.id"
                                                   t-att-data-value_name="value_id.name"
                                                   t-att-data-attribute_name="value_id.attribute_id.name"
                                                   t-att-data-is_custom="value_id.is_custom"/>
                                        </label>
                                    </li>
                                </ul>
                            </t>
                        </li>
                    </t>
                </ul>
            </xpath>
        </template>
    </data>
</odoo>